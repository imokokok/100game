import { NextRequest,NextResponse } from "next/server";
import { d1,isOwner,participantId } from "../_shared";
export async function GET(req:NextRequest){const participant=await participantId(req);if(!participant&&!isOwner(req))return NextResponse.json({error:"Invitation required"},{status:401});const [tasks,submitted,journal,links,groups,survey]=await d1().batch([d1().prepare("SELECT COUNT(*) AS n FROM tasks WHERE status = 'active'"),d1().prepare("SELECT COUNT(*) AS n FROM submissions WHERE participant_id = ? AND status = 'submitted'").bind(participant??"owner"),d1().prepare("SELECT COUNT(*) AS n FROM journal_entries"),d1().prepare("SELECT COUNT(*) AS n FROM game_links"),participant?d1().prepare("SELECT COUNT(*) AS n FROM group_members WHERE participant_id = ?").bind(participant):d1().prepare("SELECT COUNT(*) AS n FROM groups"),d1().prepare("SELECT COUNT(*) AS n FROM survey_forms WHERE status = 'active'")]);const count=(x:D1Result)=>Number((x.results?.[0] as {n?:number})?.n??0);return NextResponse.json({activeTasks:count(tasks),submittedTasks:count(submitted),journalEntries:count(journal),gameLinks:count(links),groups:count(groups),activeSurveys:count(survey)})}
